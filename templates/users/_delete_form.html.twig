<form class="display-inline" method="post" action="{{ path('app_users_delete', {'id': user.id}) }}" onsubmit="return confirm('Está segur que vol eliminar aquest usuari?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-primary"><i data-feather="delete"></i></button>
</form>
