<form class="display-inline" method="post" action="{{ path('app_productes_delete', {'idProducte': producte.idProducte}) }}" onsubmit="return confirm('Está segur que vol eliminar aquest producte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producte.idProducte) }}">
    <button class="btn btn-primary"><i data-feather="delete"></i></button>
</form>
