<form class="display-inline" method="post" action="{{ path('app_comandes_delete', {'idComanda': comanda.idComanda}) }}" onsubmit="return confirm('Estas segur que vols eliminar aquesta comanda?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comanda.idComanda) }}">
    <button class="btn btn-primary"><i data-feather="delete"></i></button>
</form>
